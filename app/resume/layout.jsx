export const metadata = {
  title: "Ismael Rengifo | Resume",
  description: "Resume — experience, education, and skills.",
};

export default function ResumeLayout({ children }) {
  return children;
}
